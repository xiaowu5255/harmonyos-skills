# 测试提示词 — file-system

> 种子用例,取自仓库级回归集 `tools/evals/evals.json`。可按需补充边界条件 / 错误处理场景。

## 基础功能测试

### 场景 1
**提示词**：用户从相册选了一张图片拿到 URI,我想复制到应用沙箱里再处理,怎么写?

**预期输出**：触发 file-system;给出 URI→沙箱拷贝路径;提示 Picker 返回 URI 的访问时效约束
